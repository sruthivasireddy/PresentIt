<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.presentit.app.WelcomeActivity"
    tools:showIn="@layout/activity_welcome">

    <TextView
        android:id="@+id/txtWelcome"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textColor="#B78877"
        android:text="Welcome"
        android:gravity="center_horizontal"
        android:layout_marginTop="100dp"
        android:layout_alignParentBottom="@id/sign_in_button"
        android:textSize="20dp" />

    <TextView
        android:id="@+id/txtUserDetails"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textColor="#B78877"
        android:text="Welcome"
        android:gravity="center_horizontal"
        android:layout_marginTop="150dp"
        android:layout_alignParentBottom="@id/sign_in_button"
        android:textSize="20dp" />

    <ImageView
        android:id="@+id/imgUserDisplayImage"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:scaleType="center"
        android:layout_below="@id/txtUserDetails"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:src="#FFF" />

</RelativeLayout>
